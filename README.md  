# Reranker Training Pipeline

基於 BGE-Reranker 的領域微調訓練框架。

## 專案成果

在國圖博碩士論文檢索任務上，**超過 BGE-Reranker-Large 達 5%**：

| 指標 | Our Model | BGE-Reranker-Large | 差距 |
|------|-----------|-------------------|------|
| Accuracy | 91.28% | 86.63% | **+4.65%** |
| Recall@1 | 78.43% | 73.20% | **+5.23%** |
| Recall@5 | 92.81% | 92.81% | +0.00% |
| Recall@10 | 96.08% | 96.73% | -0.65% |

在 C-MTEB 標準評估（僅差 1%）：

| 任務 | Our Model | BGE-Large |
|------|-----------|-----------|
| T2Reranking | 70.56% | 70.98% |
| MMarcoReranking | 70.00% | 73.00% |
| CMedQAv1 | 98.60% | 98.60% |
| CMedQAv2 | 98.20% | 98.80% |

## 特色

- **通用框架**：修改 CONFIG 即可適用於任何領域資料
- **BM25 Hard Negative**：自動生成高品質負樣本，提升訓練效果
- **多進程加速**：支援 32+ workers 平行處理
- **基本評估**：C-MTEB 標準評估 + 領域評估 + QA Benchmark
- **一鍵執行**：Pipeline 自動完成資料處理、訓練、評估

## 技術架構
```
原始領域資料
    ↓
[BM25 Hard Negative 生成] ← 多進程加速
    ↓
與 BGE 官方資料合併（領域資料重複 N 次平衡資料量）
    ↓
[分散式訓練] ← DeepSpeed ZeRO Stage 2, 64 GPUs
    ↓
訓練完成的 Reranker 模型
    ↓
[評估] ← C-MTEB + 領域評估 + QA Benchmark
```

## 📦 訓練資料

| 資料來源 | 筆數 | 說明 |
|----------|------|------|
| BGE 官方資料 | 2,019,609 | 中英文通用檢索 |
| 國圖論文資料 | 156,005 × 5 | 108-112 學年度博碩士論文 |
| **合併總計** | **2,800,000+** | 論文佔比 28% |

## 🚀 快速開始

### 安裝
```bash
pip install -r requirements.txt
```

### 方式一：一鍵執行 Pipeline
```bash
bash scripts/train_final.sh
```

自動依序執行：
1. 資料處理與合併
2. 模型訓練
3. 模型評估

### 方式二：分步執行
```bash
# Step 1: 資料處理與合併
sbatch scripts/merge_data.sh

# Step 2: 訓練
sbatch scripts/train_large.sh

# Step 3: 評估
sbatch scripts/eval2.sh
```

## ⚙️ 自訂領域資料

### 1. 準備資料

資料格式為 JSONL，每行一筆：
```json
{"query欄位": "查詢內容", "positive欄位": "相關文檔"}
```

### 2. 修改設定

編輯 `scripts/merge_training_data.py` 中的 CONFIG：
```python
CONFIG = {
    "domain_data_dir": "/path/to/your/data",    # 你的資料目錄
    "query_field": "query欄位名稱",          # Query 欄位
    "positive_field": "positive欄位名稱",    # Positive 欄位
    "domain_repeat": 5,                          # 資料重複次數（資料少就調高）
    "num_negatives": 7,                          # 負樣本數量
    "bm25_top_k": 10,                            # BM25 候選範圍
    "num_workers": 32,                           # 多進程數量
}
```

### 3. 執行
```bash
bash scripts/train_final.sh
```

## 專案結構
```
├── scripts/
│   ├── merge_data.sh            # 資料處理 SLURM 腳本
│   ├── train_large.sh           # 訓練腳本
│   ├── train_final.sh           # 完整 Pipeline（一鍵執行）(slurm)
│   ├── eval2.sh                 # 評估腳本
│   ├── eval_cmteb.sh            # cmteb評估腳本
│   ├── eval_qa_benchmark_v2.py  # 自身題目處理並評估
│   ├── pipeline.py              # 完整pipeline (local)
│   ├── upload_model.py          # 上傳程式
|
├── src/
│   ├── merge_data_v2.py         # 資料處理並合併(通用版)
│   ├── merge_data.py            # 本案使用版本
│   ├── prepare_data_v2.py       # 通用資料預處理腳本
│   ├── test.py                  # BGE-Reranker 測試腳本
│   ├── train.py                 # 
|
├── configs/
│   └── ds_config.json           # DeepSpeed 設定
├── requirements.txt
└── README.md
```

## 方法

### 兩階段檢索架構
```
使用者查詢
    ↓
[Embedder] 快速從 10 萬篇中篩出 top 100
    ↓
[Reranker] 精確重新排序，選出 top 10  ← 本專案訓練的模型
    ↓
返回結果
```

| 模型 | 速度 | 精準度 | 用途 |
|------|------|--------|------|
| Embedder | 快 | 普通 | 大規模初篩 |
| Reranker | 慢 | 高 | 小規模精排 |

### BM25 Hard Negative

傳統隨機負樣本太簡單，模型學不到東西。使用 BM25 找出「相似關鍵字但語意不同」的文檔作為負樣本：
```
Query: "浦朗克 新古典主義 鋼琴"

Random Negative: "半導體製程研究..." → 太簡單，模型輕鬆區分
BM25 Hard Negative: "德布西印象主義鋼琴..." → 有挑戰性，模型學到更多 ✓
```

### 資料平衡策略

BGE 資料 200 萬筆 vs 領域資料 15 萬筆，比例（13:1）。透過重複領域資料 5 次，將佔比提升至 28%，讓模型學到足夠的領域知識，同時保持通用能力。

## 評估指標

### C-MTEB 標準評估
- **T2Reranking**: 中文通用檢索（阿里巴巴）
- **MMarcoReranking**: 多語言網頁檢索（微軟）
- **CMedQAv1/v2**: 中文醫療問答

### 領域評估
- **Accuracy**: 正確摘要分數 > 錯誤摘要分數的比例

### QA Benchmark
- **Recall@K**: 正確答案在前 K 名的比例
- 使用人工標註的問答對，更接近真實使用場景

## 🛠️ 訓練參數

| 參數 | 值 | 說明 |
|------|-----|------|
| Base Model | xlm-roberta-large | 560M 參數 |
| Learning Rate | 3e-6 | |
| Batch Size | 1 per GPU | |
| Gradient Accumulation | 16 | |
| Train Group Size | 8 | 1 pos + 7 neg |
| Max Query Length | 64 | |
| Max Passage Length | 256 | |
| Epochs | 1 | |
| GPUs | 4 nodes × 8 = 32 | |
| DeepSpeed | ZeRO Stage 2 | |

## 授權

MIT License

## 🙏 致謝

- [FlagEmbedding](https://github.com/FlagOpen/FlagEmbedding) - BGE 模型與訓練框架
- [國家圖書館](https://www.ncl.edu.tw/) - 博碩士論文資料